# Requirements for local_model_generate.py
# Install with: pip install -r local_requirements.txt
#
# Note: torch installation varies by platform.
# CUDA:  pip install torch --index-url https://download.pytorch.org/whl/cu121
# MPS/CPU (macOS): pip install torch  (default PyPI wheel includes MPS support)

ollama>=0.3.0
transformers>=4.44.0
torch>=2.2.0
diffusers>=0.30.0
accelerate>=0.33.0
peft>=0.12.0
piper-tts>=1.2.0
pathvalidate>=3.0.0
soundfile>=0.12.0
Pillow>=10.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
